version: 3

tasks:

  system:
    desc: system info
    silent: true
    cmds:
    - echo "{{OS}}-{{ARCH}}"

  update-cli:
    desc: update CLI (TODO)
    silent: true
    cmds:
    - echo "Trying to update ops..."


  check-operator-version:
    desc: check running operator version and warns it is obsolete (TODO)
    silent: true
    cmds:
    - echo "TODO checking the running operator version"

